# ----------------------------------------------------------------------
# Airflow 및 개발 환경 관련 파일
# ----------------------------------------------------------------------

# Airflow에서 사용하는 데이터베이스 볼륨 (Postgres)
postgres-db-volume/

# Airflow 실행 중 생성되는 로그 파일 및 디렉토리
logs/

# Airflow가 초기화하며 생성하는 설정 파일
# (보안 정보, DB 연결 정보 등 민감한 정보 포함 가능)
config/airflow.cfg

# Docker 환경 변수 파일 (DB 비밀번호, Fernet Key 등)
.env

# Pip cache
.pip-cache/

# ----------------------------------------------------------------------
# 사용자 데이터 및 민감 정보 (에러 로그 기반)
# ----------------------------------------------------------------------

# 1. YouTube 쿠키 파일 (로그인 인증 정보)
# Airflow의 volumes 설정: - ./data:/opt/airflow/data 에 해당합니다.
data/cookies/

# 2. 다운로드된 비디오 파일 자체 (용량이 크고 재다운로드 가능)
# 다운로드 위치를 data/videos/ 또는 data/temp/ 등으로 설정했다면
data/videos/
data/temp/
# 특정 확장자를 가진 모든 비디오 파일 제외 (선택 사항)
# *.mp4
# *.mkv

# 3. 장면 감지 결과 파일 (pickle 파일)
# find_scenes 함수에서 생성하는 출력 파일
data/shot_list/

# ----------------------------------------------------------------------
# Python, IDE, OS 관련 파일
# ----------------------------------------------------------------------

# Python 가상 환경 디렉토리
venv/
.venv/
__pycache__/

# IDE (Visual Studio Code, PyCharm 등) 설정 파일
.vscode/
.idea/

# macOS 시스템 파일
.DS_Store